import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
	palette: {
		primary: { main: '#1ea44f' }, // Purple and green play nicely together.
		secondary: { main: '#bd4742' }, // This is just green.A700 as hex.
	},
});

export default theme;
